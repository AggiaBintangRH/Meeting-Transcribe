# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so

# Virtual Environment
.venv/
venv/
env/
ENV/

# VS Code
.vscode/

# JetBrains
.idea/

# macOS
.DS_Store

# Logs
*.log

# Cache
.cache/
.pytest_cache/
.mypy_cache/

# Jupyter
.ipynb_checkpoints/

# Hugging Face
huggingface/
hf_cache/

# Torch
torch_cache/

# Model folders (root-anchored: unanchored "models/" also matched
# Sources/MeetingTranscriber/Models/ on case-insensitive macOS)
/models/
/checkpoints/
/weights/
/pretrained_models/
/downloads/

# Audio
*.wav
*.mp3
*.flac

# Results
output/
outputs/
results/
temp/
tmp/

# Dataset
data/
datasets/

# ONNX / Torch models
*.pt
*.pth
*.ckpt
*.bin
*.onnx
*.safetensors

# Whisper cache
.whisper/

# Build
build/
dist/
*.egg-info/
/.build-cache
/.claude
/.venv-dicow
# The NeMo diarization runtime — 1.7 GB, rebuilt by download-best-models.sh 3c.
# Listed beside .venv-dicow because `.venv/` above does NOT cover it: that
# pattern is an exact directory name, not a prefix.
/.venv-nemo

# Secrets — never commit. Use .env.example as the template.
.env

# Private data: real meeting audio, transcripts, and sidecar logs.
# This project's whole premise is that recordings never leave the machine.
recordings/
logs/

# Built app bundle (multi-GB, rebuild with build.sh)
*.app/
/.build-cache

# SwiftPM build artifacts (swift build regenerates these)
.build/

# macOS cruft
.DS_Store

# Internal notes (CLAUDE.md, SESSION-HANDOFF.md, PLAN*.md) stay local —
# but the README is the repo's front page, so it ships.
*.md
!README.md
